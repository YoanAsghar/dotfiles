return {
    "nvim-neo-tree/neo-tree.nvim",
    opts = {
        window = {
            position = "left",
            width = 25, -- Change this to a smaller number (default is usually 40-50)
        },
    },
}
