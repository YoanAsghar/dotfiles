-- ============================================================================
-- CmpConfig.lua
-- lazy.nvim plugin spec for nvim-cmp (autocompletion engine)
--
-- Usage (LazyVim / lazy.nvim):
--   Place this file at lua/plugins/CmpConfig.lua
--   lazy.nvim auto-loads any file under lua/plugins/ that returns a spec table.
--
-- Pairs with LspConfig.lua (already configured to pick up cmp-nvim-lsp
-- capabilities automatically if this plugin is present).
-- ============================================================================

return {
    "hrsh7th/nvim-cmp",
}
