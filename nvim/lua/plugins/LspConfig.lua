-- ============================================================================
-- LspConfig.lua
-- lazy.nvim plugin spec for nvim-lspconfig (+ mason, mason-lspconfig, cmp-nvim-lsp)
--
-- Usage (LazyVim / lazy.nvim):
--   Place this file at lua/plugins/LspConfig.lua
--   lazy.nvim auto-loads any file under lua/plugins/ that returns a spec table,
--   so no extra `require` call is needed.
-- ============================================================================

return {
    "mason-org/mason.nvim",
}
